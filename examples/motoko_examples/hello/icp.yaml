canisters:
- name: hello
  build:
    steps:
    - type: script
      commands:
      - CANISTER_CANDID_PATH=.basilisk/hello/hello.did python -m basilisk hello src/main.py
      - cp .basilisk/hello/hello.wasm "$ICP_WASM_OUTPUT_PATH"
