canisters:
- name: whoami
  build:
    steps:
    - type: script
      commands:
      - CANISTER_CANDID_PATH=.basilisk/whoami/whoami.did python -m basilisk whoami
        src/main.py
      - cp .basilisk/whoami/whoami.wasm "$ICP_WASM_OUTPUT_PATH"
