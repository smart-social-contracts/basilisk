canisters:
- name: http_counter
  build:
    steps:
    - type: script
      commands:
      - python -m basilisk http_counter src/main.py
      - cp .basilisk/http_counter/http_counter.wasm "$ICP_WASM_OUTPUT_PATH"
  declarations:
    output: test/dfx_generated/http_counter
    node_compatibility: true
