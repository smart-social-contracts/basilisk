canisters:
- name: persistent_storage
  build:
    steps:
    - type: script
      commands:
      - python -m basilisk persistent_storage src/main.py
      - cp .basilisk/persistent_storage/persistent_storage.wasm "$ICP_WASM_OUTPUT_PATH"
  declarations:
    output: test/dfx_generated/persistent_storage
    node_compatibility: true
