canisters:
- name: factorial
  build:
    steps:
    - type: script
      commands:
      - CANISTER_CANDID_PATH=.basilisk/factorial/factorial.did python -m basilisk
        factorial src/main.py
      - cp .basilisk/factorial/factorial.wasm "$ICP_WASM_OUTPUT_PATH"
