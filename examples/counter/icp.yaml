canisters:
- name: counter
  build:
    steps:
    - type: script
      commands:
      - python -m basilisk counter src/main.py
      - cp .basilisk/counter/counter.wasm "$ICP_WASM_OUTPUT_PATH"
  declarations:
    output: test/dfx_generated/counter
    node_compatibility: true
