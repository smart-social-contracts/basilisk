canisters:
- name: optional_types
  build:
    steps:
    - type: script
      commands:
      - python -m basilisk optional_types src/main.py
      - cp .basilisk/optional_types/optional_types.wasm "$ICP_WASM_OUTPUT_PATH"
  declarations:
    output: test/dfx_generated/optional_types
    node_compatibility: true
