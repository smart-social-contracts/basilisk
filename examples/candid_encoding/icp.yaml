canisters:
- name: candid_encoding
  build:
    steps:
    - type: script
      commands:
      - CANISTER_CANDID_PATH=.basilisk/candid_encoding/candid_encoding.did python
        -m basilisk candid_encoding src/main.py
      - cp .basilisk/candid_encoding/candid_encoding.wasm "$ICP_WASM_OUTPUT_PATH"
