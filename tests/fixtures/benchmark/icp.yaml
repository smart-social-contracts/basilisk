canisters:
  - name: benchmark
    build:
      steps:
        - type: script
          commands:
            - CANISTER_CANDID_PATH=.basilisk/benchmark/benchmark.did python3 -m basilisk benchmark src/main.py
            - cp .basilisk/benchmark/benchmark.wasm "$ICP_WASM_OUTPUT_PATH"
