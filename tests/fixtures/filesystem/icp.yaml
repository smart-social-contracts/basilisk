canisters:
  - name: filesystem
    build:
      steps:
        - type: script
          commands:
            - CANISTER_CANDID_PATH=.basilisk/filesystem/filesystem.did python3 -m basilisk filesystem src/main.py
            - cp .basilisk/filesystem/filesystem.wasm "$ICP_WASM_OUTPUT_PATH"
    candid: .basilisk/filesystem/filesystem.did
