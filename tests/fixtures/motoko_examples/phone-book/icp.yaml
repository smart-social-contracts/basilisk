canisters:
  - name: phone_book
    build:
      steps:
        - type: script
          commands:
            - CANISTER_CANDID_PATH=.basilisk/phone_book/phone_book.did python3 -m basilisk phone_book src/main.py
            - cp .basilisk/phone_book/phone_book.wasm "$ICP_WASM_OUTPUT_PATH"
    candid: .basilisk/phone_book/phone_book.did
