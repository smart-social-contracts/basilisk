canisters:
  - name: browse
    build:
      steps:
        - type: script
          commands:
            - CANISTER_CANDID_PATH=.basilisk/browse/browse.did python3 -m basilisk browse src/main.py
            - cp .basilisk/browse/browse.wasm "$ICP_WASM_OUTPUT_PATH"
