canisters:
- name: benchmark_counter
  build:
    steps:
    - type: script
      commands:
      - echo 'Wasm built externally'
      - cp .basilisk/benchmark_counter/benchmark_counter.wasm "$ICP_WASM_OUTPUT_PATH"
